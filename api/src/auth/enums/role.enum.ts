export enum UserRole{
    OWNER = 'owner',
    EMPLOYEE = 'employee',
    CUSTOMER = 'customer'
}

export enum OrderStatus{
    PENDING = 'pending',
    PAID = 'paid',
    PREPARING = 'preparing',
    DELIVERED = 'delivered',
    CANCELED = 'canceled'
}